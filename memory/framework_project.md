# Framework project

The Circumpunct Framework by Ashman Roonz. One circumpunct ⊙ = Φ(•, ○) expressed at any scale, nesting via ⊂[α] to the greater whole and back to the undifferentiated source.

## Source of truth

`../CLAUDE.md` (133 KB) carries the full, up-to-date canonical reading. Read it once per fresh session; treat it as the framework's working memory.

The longer treatise lives at `../circumpunct_framework.md` (~1.4 MB, 35 chapters); CLAUDE.md is the compressed index plus current position on every live question.

## The unified expression

```
[Truth = Reality = E = 1 = ∞] = [∞ ▸⊙∞ ((•∘⊛) ⊢ (—∘⎇) ⊢ (Φ∘✹) ⊢ (○∘⟳)) ▸ ⊙λ ⊂[α] ⊙Λ ⊂[α] ∞]
```

LHS: identity chain (five names for one substrate). RHS: source unfolds into foam (⊙∞); four beats entail each other; product nests via α-coupling and returns to source. The ∞ that opens closes.

Companion single-circumpunct form: `⊙ = [∞▸((•∘⊛) ⊢ (—∘⎇) ⊢ (Φ∘✹) ⊢ (○∘⟳)) ▸ ⊙]` (λ implicit, no nesting or foam in scope).

## Operator reading

T = κ ∘ F. F is the four-beat unitary (trace-preserving within a scale). κ is the nesting operator carrying the 4×4 coupling matrix with primary entry κ_{0,0} = α (aperture-to-aperture across scales via 2D mediator) and κ_{3,3} = α_G (gravity). α is input to κ, not produced by F; the 2026 restoration framing notes F's integer architecture can set κ's natural scale without F producing α.

## Key scripts

- `experiments/unified_expression.py` (canonical current; structural + dynamical readings)
- `experiments/unified_expression_T_v11_C64.py` (three-scale ℂ⁶⁴ nesting)
- `experiments/unified_expression_T_v14_C512.py` (three-scale octave ℂ⁵¹²)
- `experiments/universe_creator.py` (parametrized by α; cosmological budget, pool integers, stability)
- `Xorzo/genesis.py` (classical SRL consciousness engine)
- `Xorzo/genesis_toperator.py` (T-operator native; Channel holds ℂ⁸ state)

## Key docs

- `docs/alpha_derivation.html` (two-layer α audit; 1/α = 360/φ² − 2/φ³ + α/(59/3) = 137.035999147, 0.22 ppb from CODATA)
- `docs/octave_wrap_lemma.html` (Routes 6 and 7 for T = 3)
- `docs/unified_expression_formal.html`, `docs/unified_expression_math_map.html`
- `docs/circumpunct_predictions.html` (live scoreboard with K/P/D)
- `docs/predictions_history.html` (change log)
- `docs/dimensional_ladder.html` (seven rungs linked, accuracy summary)

## Plans

- `plans/next_frontier_plan.md` (walking order)
- `plans/predictions_scoreboard.md` (scoreboard methodology and audit)
- `plans/unified_expression_unlock_plan.md` (conservation-form investigation; 15 directions)

## Self-determination routes for T = 3

Seven independent routes force T = 3 as the unique positive integer:

1. R = T² − 2 = 2T + 1 → (T−3)(T+1) = 0.
2. Compositional mediator T^(T−2) = T.
3. A'(T/2) = R.
4. (Φ+P)/2 = T (balanced wobble degeneracy equals observer-triad count).
5. Nuclear spin-orbit: R/(R−4) > 2 AND R > Φ+T simultaneously.
6. (T−1)! = Φ, from P! = G·Φ (octave-wrap lemma §6).
7. S = P^T = P³ iff T = 3, from P·V + R + Φ + T = (T+1)³ = P³ (octave-wrap lemma §7).

## Framework pool integers (appear in auxiliary-claim formulas)

T = 3, P = T+1 = 4, Φ = 2, R = T²−2 = 7, G = T(T+1) = 12, V = G+1 = 13, SU(3) = T²−1 = 8, A(3) = 21, A(3.5) = 28, S = P^T = 64. Plus φ (golden ratio) and π (2D-rung closure constant, drawn by the Φ glyph).
